# Archivo de configuración para pruebas (opcional)
# Copiar a `.env.test` y ajustar según tu entorno de base de datos.

PORT=3000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=universidad_app
JWT_SECRET=tu_secreto_de_prueba
JWT_EXPIRES_IN=1d
CORS_ORIGIN=http://localhost:4200
